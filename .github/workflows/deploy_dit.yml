- name: Run webapp and generate static files
  run: |
    dotnet run --project bruuteuzius.csproj  --configuration Release

- name: Deploy to GitHub Pages
  uses: JamesIves/github-pages-deploy-action@v4
  with:
    folder: ./output
